# דף נחיתה — עורך דין (בדיקה)

פתיחה: double-click על `index.html` (או גרירה לדפדפן / `file://`).

## מצב (עדכון 2026-09-15)
- דף סטטי בלבד. ציון ביקש במפורש להמציא תוכן ולא לחכות לפרטים האמיתיים — הדף מכיל עכשיו דמות בדיונית (שם, התמחות, ביו, שירותים) עם באנר וכיתוב מפורשים שזו דמות להדגמה בלבד, לא עורך דין אמיתי.
- תמונה: לא נוצרה — יצירת תמונה דורשת את `make-image.mjs` (Gemini), שנכון ל-2026-09-15 חסום ב-quota על מודלי תמונה בחשבון החינמי. נשאר אווטאר טקסטואלי (ראשי תיבות) במקום תמונה.
- טופס השארת פרטים (שם, טלפון, אימייל, הודעה) מחובר עכשיו ל-`mailto:aztodev@gmail.com` — היעד האמיתי שציון אישר. פתיחת מייל בלקוח הדואר של המבקר/ת, לא שליחה שקטה מהשרת (אין בקאנד לדף סטטי זה).
- ציון אישר את שני ה-Gates (production deploy + חשיפה פומבית) להעלאת הדף הזה כפי שהוא (בדיקה, תוכן בדיוני).
- **עדיין לא פרוס בפועל** — הקוד הזה יושב בתוך `AZToDev-HQ`, שאסור שתהפוך ריפו ציבורי (מכילה ledger/ops פנימיים — ראה `ops/company-lessons.md` → `hq_repo_public_exposure_recurrence`). כדי לפרוס בכתובת חיה וציבורית צריך ריפו נפרד למוצר, בדיוק כמו החסימה הקיימת ב-kids-math-quiz — וההרשאה הנוכחית של ה-run הזה ל-GitHub מוגבלת ל-`AZToDev-HQ` בלבד, כך שאי אפשר ליצור/לדחוף לריפו נפרד מכאן, גם אחרי אישור.

## מה חסר כדי שזה יהיה מוצר אמיתי
1. ~~הפרטים האמיתיים~~ — ציון ביקש להמציא, הומצא (2026-09-15). תמונה עדיין חסרה (quota).
2. ~~יעד לקבלת לידים מהטופס~~ — סופק: `aztodev@gmail.com` (mailto, 2026-09-15).
3. ~~אישור לפרוס בכתובת חיה (Gate)~~ — ניתן ע"י ציון (2026-09-15) — אבל עדיין חסום טכנית: צריך ריפו נפרד למוצר לפני deploy ציבורי בפועל, וזה דורש פעולה של ציון עצמו (ראה CLAUDE.md → Live links: יצירת ריפו ציבורי היא Gate + ה-run הזה לא יכול ליצור/לדחוף לריפו חדש בכלל).
